# Creating a new host
1) duplicate an already existent host
2) in configuration.nix, change the hostname to match the new host
3) check for specific files to create for that host, if you use:
    - Hyprland --> create hostname.conf at the project's path (from project root) ./hyprland/hosts-custom-configs
