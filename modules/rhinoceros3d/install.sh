#!/bin/bash

echo "Yo! I am the rhinoceros3d module's install.sh, and I received the path for the cloned repo as $1"
echo "TODOOOO this is still to do"
echo "you have to enable multilib in /etc/pacman.conf to allow 32 bit applications, then wine will be visible in pacman to install"
